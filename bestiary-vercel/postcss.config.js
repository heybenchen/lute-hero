// The bestiary uses plain hand-written CSS (see app/globals.css) with no
// Tailwind/PostCSS plugins. This local config stops Next.js from walking up
// and inheriting the parent Lute Hero repo's PostCSS/Tailwind config.
module.exports = { plugins: {} };
